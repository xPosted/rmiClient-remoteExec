package com.jubaka.remoting.model.dto;

public class Test {
}
/*
inputpath = args.infile

for dirpath, dirnames,filenames os.walk(inputpath):
    for file in in filenamse:
        path= os.path.join(dirpath+files)
        with open (path) rb)as fileread:
            for line in fileread:
                line.read()
                pos= re.search(b'\xf\00\xb',line)
                if pos != None:
                print(pos.groupe())
                print(pos.start(),pos.end())
24 34
45 44
124 645  00 00 0d



patern= r'\d{1,2}+\d{0,2}'

patlist=[]

patlist.apend(patern)

def find_Func(text, line):
"""function for " +
        "search pattern"""
    if text in line:
return text
    else:None

res = find_Func(bytes,line)

*/




